pre-commit:
  parallel: true
  commands:
    lint-staged:
      run: scripts/lint-staged.sh
    gitleaks:
      run: gitleaks git --pre-commit --staged --no-banner --redact

pre-push:
  commands:
    ci-check:
      run: make ci-check
